---
import { site } from '../site.config';
const paragraphs = site.aboutText.split(/\n\n+/).map((p) => p.trim()).filter(Boolean);
---

<section class="section">
  <div class="card">
    <h2 class="text-base lg:text-xl font-bold tracking-wide text-sunburst">What is EuphoricFM?</h2>
    {paragraphs.map((p) => <p class="mt-3 text-sm lg:text-base leading-relaxed text-cream/85">{p}</p>)}
  </div>
</section>
