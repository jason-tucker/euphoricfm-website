{
  "name": "euphoricfm-website",
  "description": "In-game phone site for EuphoricFM — a GTA5 RP pop radio station. Astro static site embedded in a FiveM CEF iframe at info.euphoric.fm, with live AzuraCast now-playing, song requests, and Discord-webhook forms.",
  "type": "module",
  "version": "0.11.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test server/*.test.mjs"
  },
  "dependencies": {
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "tailwindcss": "^4.3.2"
  },
  "pnpm": {
    "overrides": {
      "esbuild@<0.28.1": ">=0.28.1"
    }
  }
}
